import { css } from '@emotion/core';

export default css`
  * {
    box-sizing: border-box;
  }

  html,
  body {
    margin: auto 0;
    font-family: 'Lato', sans-serif;
    display: flex;
    flex-direction: column;
    text-align: center;
  }
`;
